import ReceptionAgentPortal from "@/components/ReceptionAgentPortal";

/** Espace minimal, protégé, destiné aux agents de réception (arrivée des Conduces). */
export default function ReceptionPage() {
  return <ReceptionAgentPortal />;
}
