"use client";
import PackagesEngine from "@/components/PackagesEngine";

/**
 * Paj Packages — itilize PackagesEngine (menm motè ak Modil Conduces).
 * Single Source of Truth: AUCUN kòd dwaplike ant Packages ak Conduces.
 */
export default function PackagesPage() {
  return <PackagesEngine />;
}
