import PickupAgentPortal from "@/components/PickupAgentPortal";

/** Espace minimal, protégé, destiné aux agents des points de retrait. */
export default function EspaceRemisePage() {
  return <PickupAgentPortal />;
}
